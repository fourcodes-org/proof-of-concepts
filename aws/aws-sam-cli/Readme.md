

```bash

yum install wget unzip zip
wget https://github.com/aws/aws-sam-cli/releases/latest/download/aws-sam-cli-linux-x86_64.zip
unzip aws-sam-cli-linux-x86_64.zip  -d aws-sam-cli-linux
cd aws-sam-cli-linux
sudo ./install
sam --version

```
